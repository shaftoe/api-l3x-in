# Pandoc binary is 20Mb so we don't keep in in the repository

`pandoc` binary must be downloaded and put below `bin/` folder

## Latest version of Pandoc

Release page: <https://github.com/jgm/pandoc/releases/>
